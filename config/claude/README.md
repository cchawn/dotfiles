# Claude Code CLI Configuration

Configuration for [Claude Code](https://docs.anthropic.com/en/docs/claude-code), Anthropic's CLI for Claude.

Configuration files are symlinked to `$HOME/.claude` so that Claude Code will automatically use this user-level configuration.
